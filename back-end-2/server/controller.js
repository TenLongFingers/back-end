const getHouses = (res, req) => {};
const deletHouse = (res, req) => {};
const createHouse = (res, req) => {};
//app.post(`${baseURL}/api/new-house, (req, res) => {
//    let { address, price, img} = req.body
//
// if (!address || !price ||) {
// res.status(400).send(`Please include at least the address and the price of the house`)
//} else {
// houseDatabase.push(req.body)}
//res.status(200).send(`Congratulations! Your house is now officially for sale!`)
//})
const updateHouse = (res, req) => {};
